ps

Used in unix to display current running processes initiated by the current user.

Generally used with the '-A' flag, to see all processes.

Note: the first column PID number is often noted, so a user can enter this into the kill command to end a process!
